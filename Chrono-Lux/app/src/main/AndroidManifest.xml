<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adam.chrono_lux">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MyApplicationActivity"
            android:label="@string/txt_selectbridges" />
        <activity android:name=".Hue.PHPushlinkActivity"
            android:label="@string/app_name"/>
        <activity android:name=".Hue.PHHomeActivity"
            android:label="@string/app_name"/>
        <activity android:name=".Hue.PHHubSetup"
            android:label="Get Name"/>

    </application>

</manifest>